(function($){ 
	// Remove the textarea before displaying visual editor
	$('#description').parents('tr').remove();

	// Change State if not Brasil
	//$( "#country" ).change(function() {
	//    var str = "";
	//    $( "select option:selected" ).each(function() {
	//      str += $( this ).text() + " ";
	//    });
	//    $( "#state" ).text( str );
	//  })
	//  .trigger( "change" );

})(jQuery);